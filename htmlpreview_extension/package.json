{
  "name": "testext_g4d",
  "displayName": "Your Extension Name",
  "description": "Your extension description",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.x.x"
  },
  "activationEvents": [
    "onCommand:myExtension.openWithLiveServer",
    "onCommand:myExtension.openFile"
  ],
  "contributes": {
    "commands": [
      {
        "command": "myExtension.openWithLiveServer",
        "title": "Open with Live Server"
      },
      {
        "command": "myExtension.openFile",
        "title": "Open File with Live Server"
      }
    ]
  }
}